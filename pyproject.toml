[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "arena"
version = "0.0.1.dev0"
description = "A place to putz with Python."
authors = [
    { name = "Tony Lechner", email = "tony@tony-lechner.com" },
]
dependencies = []

[project.scripts]
arena = "arena.__main__:main"

[tool.mypy]
strict = true

[tool.setuptools.packages.find]
where = ["src"]
